name: Build SeedSigner OS (Luckfox Pico)

# The Luckfox matrix is expensive (5 combos, ~1-2h each), so auto-runs are scoped
# to Luckfox-relevant paths. Use "Run workflow" (workflow_dispatch) for on-demand builds.
on:
  push:
    branches: [ main, dev ]
    paths:
      - 'opt/luckfox/**'
      - 'opt/external-packages/**'
      - '.github/workflows/build-luckfox.yml'
  pull_request:
    paths:
      - 'opt/luckfox/**'
      - 'opt/external-packages/**'
      - '.github/workflows/build-luckfox.yml'
  workflow_dispatch:
    inputs:
      hardware_type:
        description: 'Hardware Type (ALL builds every valid hardware/boot combination)'
        required: true
        type: choice
        options:
          - ALL
          - RV1103_Luckfox_Pico_Mini
          - RV1106_Luckfox_Pico_Pro_Max
          - RV1106_Luckfox_Pico_Pi
        default: 'RV1103_Luckfox_Pico_Mini'
      boot_medium:
        description: 'Boot Medium (ALL builds every valid boot medium for the selected hardware)'
        required: true
        type: choice
        options:
          - ALL
          - SD_CARD
          - SPI_NAND
          - EMMC
        default: 'SD_CARD'
      disable_uart2_console_debug:
        description: 'Serial console (ttyFIQ0) override: auto follows build_variant (non-dev=off, dev=on); or force true/false'
        required: false
        type: choice
        options:
          - 'auto'
          - 'true'
          - 'false'
        default: 'auto'
      usb_mode:
        description: 'USB role: host = drive USB peripherals (camera/smartcard), no adb/RNDIS (air-gapped); gadget = USB device mode with adb; otg = dual-role (boots as gadget/adb, runtime-switchable to host — for testing). auto follows build_variant (non-dev=host, dev=gadget).'
        required: false
        type: choice
        options:
          - 'auto'
          - 'gadget'
          - 'host'
          - 'otg'
        default: 'auto'
      debug_network:
        description: 'Ethernet debug channel (telnet/ssh root shell over the network). on = keep it (debugging); off = remove it (air-gapped). auto follows build_variant (non-dev=off, dev=on).'
        required: false
        type: choice
        options:
          - 'auto'
          - 'on'
          - 'off'
        default: 'auto'
      harden_adb:
        description: 'Strip the adb userspace on non-dev (stubs adbd/usbdevice, blanks gadget config). on = strip (default); off = leave it. Bisect lever: dr_mode=host already blocks adb, so off is still adb-free but skips the rootfs surgery.'
        required: false
        type: choice
        options:
          - 'on'
          - 'off'
        default: 'on'
      readonly_rootfs:
        description: 'Read-only root filesystem: squashfs + tmpfs overlays on /etc /var /root /home, so a dirty unmount cannot corrupt / and no change to / survives a reboot. /userdata stays writable. auto follows build_variant (non-dev=on, dev=off).'
        required: false
        type: choice
        options:
          - 'auto'
          - 'on'
          - 'off'
        default: 'auto'
      boot_log:
        description: 'Persistent boot log (/userdata/seedsigner-boot.log): on = the build bakes /etc/seedsigner-boot-log and start-seedsigner.sh records every boot (rotated, 128 KiB cap, deleted on a successful boot); off = the log is never initialized and the device writes nothing to flash (default). Off by default so production images stay write-free; enable only for an instrumented/debug build.'
        required: false
        type: choice
        options:
          - 'on'
          - 'off'
        default: 'off'
      testing_build:
        description: 'Testing build (off by default): ships /etc/seedsigner-testing-build, which swaps the Home menu for the hardware test menu (I/O Test, Test Smartcard, Flash Applet, Settings). For hardware bring-up only -- NOT a production image.'
        required: false
        type: choice
        options:
          - 'off'
          - 'on'
        default: 'off'
      build_variant:
        description: 'Build variant: non-dev (hardened/air-gapped: no serial login, no USB adb/RNDIS, no logging, no dev tools) or dev (serial console + adb shell). Automatic push/PR CI always builds dev.'
        required: false
        type: choice
        options:
          - 'non-dev'
          - 'dev'
        default: 'non-dev'
      luckfox_repo_url:
        description: 'LuckFox Pico SDK Repository URL'
        required: false
        type: string
        default: 'https://github.com/3rdIteration/luckfox-pico.git'
      luckfox_branch:
        description: 'LuckFox Pico SDK Branch (leave empty for default branch)'
        required: false
        type: string
        default: ''
      seedsigner_repo_url:
        description: 'SeedSigner Repository URL'
        required: false
        type: string
        default: 'https://github.com/3rdIteration/seedsigner.git'
      seedsigner_branch:
        description: 'SeedSigner app ref: a branch, a RELEASE TAG (e.g. SeSi-0.8.7+ShSi-B11), or a commit. Anything `git clone -b` accepts. A tag is the only way to pin the app, which is the one component this repo does not fix — the resolved tag and commit are recorded in /etc/seedsigner-os-release.'
        required: false
        type: string
        default: 'dev'
      build_rust_from_source:
        description: 'Build Rust toolchain from source (ignore cached binary). Use when updating Rust version.'
        required: false
        type: boolean
        default: false
      release_tag:
        description: 'Upload artifacts to a GitHub Release with this tag (leave empty to skip release upload)'
        required: false
        type: string
        default: ''

  # Allow other repos (e.g. the seedsigner app) to reuse this build. Inputs mirror
  # workflow_dispatch (as strings, since workflow_call has no `choice` type) plus
  # seedsigner_os_repo/ref for the reusable checkout.
  workflow_call:
    inputs:
      hardware_type:
        required: false
        type: string
        default: 'RV1103_Luckfox_Pico_Mini'
      boot_medium:
        required: false
        type: string
        default: 'SD_CARD'
      disable_uart2_console_debug:
        required: false
        type: string
        default: 'auto'
      usb_mode:
        required: false
        type: string
        default: 'auto'
      debug_network:
        required: false
        type: string
        default: 'auto'
      harden_adb:
        required: false
        type: string
        default: 'on'
      readonly_rootfs:
        required: false
        type: string
        default: 'auto'
      boot_log:
        required: false
        type: string
        default: 'off'
      testing_build:
        required: false
        type: string
        default: 'off'
      build_variant:
        required: false
        type: string
        default: 'non-dev'
      luckfox_repo_url:
        required: false
        type: string
        default: 'https://github.com/3rdIteration/luckfox-pico.git'
      luckfox_branch:
        required: false
        type: string
        default: ''
      seedsigner_repo_url:
        required: false
        type: string
        default: 'https://github.com/3rdIteration/seedsigner.git'
      seedsigner_branch:
        required: false
        type: string
        default: 'dev'
      build_rust_from_source:
        required: false
        type: boolean
        default: false
      release_tag:
        required: false
        type: string
        default: ''
      seedsigner_os_repo:
        description: 'seedsigner-os repository to build from (owner/name)'
        required: false
        type: string
        default: ''
      seedsigner_os_ref:
        description: 'seedsigner-os ref (branch/tag/sha) to build from'
        required: false
        type: string
        default: ''

jobs:
  build:
    name: Build (${{ matrix.hardware }} / ${{ matrix.boot }})
    runs-on: ubuntu-22.04
    timeout-minutes: 240  # 4 hours max
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # When manually triggered via workflow_dispatch with specific (non-ALL) inputs, build that single combination.
          # Otherwise (push, pull_request, or ALL selected), build all 5 valid combinations.
          ${{ (inputs.hardware_type != '' && inputs.hardware_type != 'ALL' && inputs.boot_medium != '' && inputs.boot_medium != 'ALL') && fromJSON(format('[{{"hardware":"{0}","boot":"{1}"}}]', inputs.hardware_type, inputs.boot_medium)) || fromJSON('[
            {"hardware":"RV1103_Luckfox_Pico_Mini","boot":"SD_CARD"},
            {"hardware":"RV1103_Luckfox_Pico_Mini","boot":"SPI_NAND"},
            {"hardware":"RV1106_Luckfox_Pico_Pro_Max","boot":"SD_CARD"},
            {"hardware":"RV1106_Luckfox_Pico_Pro_Max","boot":"SPI_NAND"},
            {"hardware":"RV1106_Luckfox_Pico_Pi","boot":"EMMC"}
          ]') }}
    
    steps:
    - name: Checkout seedsigner-os repository
      uses: actions/checkout@v4
      with:
        # Defaults target this repo/ref for direct push/dispatch runs; a caller
        # (e.g. the seedsigner app's build-luckfox workflow) overrides these to
        # build a specific seedsigner-os checkout.
        repository: ${{ inputs.seedsigner_os_repo || github.repository }}
        ref: ${{ inputs.seedsigner_os_ref || github.ref }}
        path: seedsigner-os

    - name: Validate build combination
      run: |
        HARDWARE="${{ matrix.hardware }}"
        BOOT="${{ matrix.boot }}"

        echo "Validating build combination: $HARDWARE / $BOOT"

        case "$HARDWARE:$BOOT" in
          "RV1103_Luckfox_Pico_Mini:SD_CARD" | \
          "RV1103_Luckfox_Pico_Mini:SPI_NAND" | \
          "RV1106_Luckfox_Pico_Pro_Max:SD_CARD" | \
          "RV1106_Luckfox_Pico_Pro_Max:SPI_NAND" | \
          "RV1106_Luckfox_Pico_Pi:EMMC")
            echo "✅ Valid combination"
            ;;
          *)
            echo "❌ Invalid combination: $HARDWARE / $BOOT"
            echo "Valid combinations are:"
            echo "  - RV1103_Luckfox_Pico_Mini: SD_CARD or SPI_NAND"
            echo "  - RV1106_Luckfox_Pico_Pro_Max: SD_CARD or SPI_NAND"
            echo "  - RV1106_Luckfox_Pico_Pi: EMMC"
            exit 1
            ;;
        esac

    - name: Free up disk space
      run: |
        sudo apt-get clean
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/local/share/boost
        df -h

    # ---------------------------------------------------------------------
    # From here the build runs inside the SAME Docker image a developer uses
    # locally (opt/luckfox/Dockerfile -> os-build.sh), driven by the SAME
    # wrapper (opt/luckfox/build.sh). This replaces ~1450 lines of build steps
    # that were inlined here and duplicated, with drift, in os-build.sh and
    # build-local.sh -- three implementations of one build. Reproducibility is
    # impossible while CI and a local developer run different code, so this is
    # a prerequisite for it, not a tidy-up. Mirrors how build.yml drives the
    # Pi/La Frite build via docker compose.
    # ---------------------------------------------------------------------

    - name: Prepare build metadata
      id: meta
      run: |
        set -euo pipefail

        # Matrix -> build.sh arguments. build.sh special-cases the Pico Pi,
        # which boots eMMC and nothing else, so it needs no medium flag.
        case "${{ matrix.hardware }}" in
          RV1103_Luckfox_Pico_Mini)     MODEL=mini ;;
          RV1106_Luckfox_Pico_Pro_Max)  MODEL=max  ;;
          RV1106_Luckfox_Pico_Pi)       MODEL=pi   ;;
          *) echo "Unknown hardware: ${{ matrix.hardware }}" >&2; exit 1 ;;
        esac

        case "${{ matrix.boot }}" in
          SD_CARD)  MEDIA="--microsd" ;;
          SPI_NAND) MEDIA="--nand"    ;;
          EMMC)     MEDIA=""          ;;
          *) echo "Unknown boot medium: ${{ matrix.boot }}" >&2; exit 1 ;;
        esac

        echo "model=$MODEL"  >> "$GITHUB_OUTPUT"
        echo "media=$MEDIA"  >> "$GITHUB_OUTPUT"

        # The pinned SDK revision belongs in the Rust cache key: the SDK pins
        # the Buildroot revision, which pins the Rust version. Keying on the
        # defconfig alone can hand a build a toolchain from a different
        # Buildroot.
        SDK_COMMIT=$(grep -vE '^[[:space:]]*(#|$)' seedsigner-os/opt/luckfox/SDK_COMMIT | head -1 | tr -d '[:space:]')
        echo "sdk_commit=$SDK_COMMIT" >> "$GITHUB_OUTPUT"
        echo "Pinned LuckFox SDK: $SDK_COMMIT"

        # seedsigner-os provenance for /etc/seedsigner-os-release. There is no
        # seedsigner-os checkout inside the container, so these have to be
        # resolved here and forwarded; without them the image records "unknown".
        {
          echo "SEEDSIGNER_OS_REPO=https://github.com/${{ inputs.seedsigner_os_repo || github.repository }}"
          echo "SEEDSIGNER_OS_BRANCH=$(git -C seedsigner-os rev-parse --abbrev-ref HEAD)"
          echo "SEEDSIGNER_OS_COMMIT=$(git -C seedsigner-os rev-parse HEAD)"
          echo "SEEDSIGNER_OS_DATE=$(git -C seedsigner-os log -1 --format=%cI)"
        } >> "$GITHUB_ENV"

    - name: Restore Rust toolchain cache
      if: ${{ inputs.build_rust_from_source != true }}
      uses: actions/cache@v4
      with:
        path: luckfox-cache
        # v3: the key gained the SDK commit (see above). Bumping the prefix
        # retires v2 entries, which were keyed on the defconfig alone.
        key: luckfox-rust-v3-${{ steps.meta.outputs.sdk_commit }}-${{ hashFiles('seedsigner-os/opt/luckfox/configs/luckfox_pico_defconfig') }}
        restore-keys: |
          luckfox-rust-v3-${{ steps.meta.outputs.sdk_commit }}-

    - name: Build image in Docker
      env:
        # Consumed by opt/luckfox/build.sh's env passthrough. Passing the repo
        # URL this way (rather than as a flag) is what makes fork PRs work.
        SEEDSIGNER_REPO_URL: ${{ inputs.seedsigner_repo_url || 'https://github.com/3rdIteration/seedsigner.git' }}
      run: |
        set -euo pipefail
        cd seedsigner-os/opt/luckfox

        # --repos-dir puts the SDK + app checkouts on the runner filesystem
        # instead of a named Docker volume, which actions/cache cannot see and
        # a post-build inspection step cannot read.
        #
        # 'auto' is passed through verbatim for every tri-state lever: build.sh
        # and os-build.sh resolve it against the build variant using the same
        # rule this workflow used to implement inline.
        ./build.sh build \
          --model "${{ steps.meta.outputs.model }}" \
          ${{ steps.meta.outputs.media }} \
          --output       "$GITHUB_WORKSPACE/build-artifacts" \
          --repos-dir    "$GITHUB_WORKSPACE/luckfox-repos" \
          --cache-dir    "$GITHUB_WORKSPACE/luckfox-cache" \
          --variant         "${{ inputs.build_variant || 'dev' }}" \
          --usb-mode        "${{ inputs.usb_mode || 'auto' }}" \
          --debug-network   "${{ inputs.debug_network || 'auto' }}" \
          --readonly-rootfs "${{ inputs.readonly_rootfs || 'auto' }}" \
          --boot-log        "${{ inputs.boot_log || 'off' }}" \
          --harden-adb      "${{ inputs.harden_adb || 'on' }}" \
          --testing-build   "${{ inputs.testing_build || 'off' }}" \
          --seedsigner-ref  "${{ inputs.seedsigner_branch || 'dev' }}"

    - name: Fix permissions
      if: always()
      run: |
        # The container runs as root, so everything it wrote is root-owned and
        # the upload/summary steps below cannot read it. Same fix as
        # build-buildroot.yml in the seedsigner repo.
        sudo chown -R "$USER:$USER" build-artifacts luckfox-cache 2>/dev/null || true

    - name: Free space before artifact upload
      if: always()
      run: |
        # The SDK checkout is ~37 GB and is not an artifact. Dropping it before
        # the upload steps keeps the runner from filling up.
        sudo rm -rf luckfox-repos || true
        df -h

    - name: List build artifacts
      if: always()
      run: |
        echo "🎯 Build artifacts:"
        ls -lah build-artifacts/ 2>/dev/null || echo "No build artifacts directory found"
        
        if [ -d build-artifacts ]; then
          echo ""
          echo "📦 SD Card Images (.img):"
          find build-artifacts -name "*.img" -type f 2>/dev/null | while IFS= read -r f; do
            echo "  $(basename "$f") - $(du -sh "$f" | cut -f1)"
          done || echo "  No SD images found"
          
          echo ""
          echo "📦 NAND Bundles (.tar.gz):"
          find build-artifacts -name "*.tar.gz" -type f 2>/dev/null | while IFS= read -r f; do
            echo "  $(basename "$f") - $(du -sh "$f" | cut -f1)"
          done || echo "  No NAND bundles found"
        fi

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: seedsigner-luckfox-pico-${{ matrix.hardware }}-${{ matrix.boot }}-${{ github.run_number }}.zip
        path: build-artifacts/
        retention-days: 30
        if-no-files-found: warn

    # Uploaded separately from the images so the aggregation job below can
    # collect every combination's hashes without pulling down ~100 MB each.
    - name: Upload checksum manifest
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: sha256-${{ matrix.hardware }}-${{ matrix.boot }}
        path: build-artifacts/sha256sums.txt
        retention-days: 90
        if-no-files-found: warn
    
    - name: Upload artifacts to GitHub Release
      if: success() && github.event_name == 'workflow_dispatch' && inputs.release_tag != ''
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        TAG="${{ inputs.release_tag }}"

        echo "🏷️ Uploading artifacts to release: $TAG"

        # Create the release if it doesn't already exist.
        # Multiple matrix jobs may race here; if creation fails because
        # another job already created it, the retry loop will pick it up.
        if ! gh release view "$TAG" --repo "${{ github.repository }}" > /dev/null 2>&1; then
          echo "📦 Creating release $TAG..."
          CREATE_OUTPUT=$(gh release create "$TAG" \
            --repo "${{ github.repository }}" \
            --title "Release $TAG" \
            --notes "Build artifacts for SeedSigner LuckFox Pico - $TAG" \
            --draft=false 2>&1) && echo "📦 Release created" || echo "⚠️ Release creation failed (may have been created by another job): $CREATE_OUTPUT"
        fi

        # Verify the release exists, retrying a few times to handle race conditions
        ATTEMPTS=0
        MAX_ATTEMPTS=5
        while ! gh release view "$TAG" --repo "${{ github.repository }}" > /dev/null 2>&1; do
          ATTEMPTS=$((ATTEMPTS + 1))
          if [ "$ATTEMPTS" -ge "$MAX_ATTEMPTS" ]; then
            echo "❌ Failed to create or find release $TAG after $MAX_ATTEMPTS attempts"
            exit 1
          fi
          echo "⏳ Release not found yet, retrying ($ATTEMPTS/$MAX_ATTEMPTS)..."
          sleep "$((ATTEMPTS * 2))"
        done

        echo "✅ Release $TAG is ready"

        # Collect files to upload
        UPLOAD_FILES=()

        # SD card images are top-level .img files
        while IFS= read -r -d '' f; do
          UPLOAD_FILES+=("$f")
        done < <(find build-artifacts -maxdepth 1 -name "*.img" -type f -print0 2>/dev/null)

        # NAND and eMMC bundles are directories — zip them for release
        while IFS= read -r -d '' d; do
          DIRNAME=$(basename "$d")
          echo "📦 Zipping bundle directory: $DIRNAME"
          (cd build-artifacts && zip -r "${DIRNAME}.zip" "$DIRNAME")
          UPLOAD_FILES+=("build-artifacts/${DIRNAME}.zip")
        done < <(find build-artifacts -maxdepth 1 -mindepth 1 -type d -print0 2>/dev/null)

        if [ ${#UPLOAD_FILES[@]} -eq 0 ]; then
          echo "⚠️ No files found to upload to release"
          exit 0
        fi

        # Upload each file, skipping assets that already exist in the release
        EXISTING_ASSETS=$(gh release view "$TAG" --repo "${{ github.repository }}" --json assets --jq ".assets[].name" 2>/dev/null || true)
        for f in "${UPLOAD_FILES[@]}"; do
          BASENAME=$(basename "$f")
          echo "⬆️  Uploading: $BASENAME"

          if echo "$EXISTING_ASSETS" | grep -qx "$BASENAME"; then
            echo "⏭️  Asset '$BASENAME' already exists in release $TAG, skipping"
          else
            gh release upload "$TAG" "$f" --repo "${{ github.repository }}" || {
              echo "❌ Failed to upload $BASENAME"
              exit 1
            }
            echo "✅ Uploaded: $BASENAME"
          fi
        done

    - name: Build summary
      if: always()
      run: |
        echo "## 🎯 SeedSigner Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Hardware**: ${{ matrix.hardware }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Boot Medium**: ${{ matrix.boot }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Run Number**: ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
        
        # Find SD card images
        SD_IMAGES=$(find build-artifacts -name "*.img" -type f 2>/dev/null || true)
        NAND_BUNDLES=$(find build-artifacts -name "*-nand-bundle-*.tar.gz" -type f 2>/dev/null || true)
        EMMC_BUNDLES=$(find build-artifacts -name "*-emmc-files-*" -type d 2>/dev/null || true)
        
        if [ -n "$SD_IMAGES" ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 💾 SD Card Images (Ready to Flash)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "$SD_IMAGES" | while IFS= read -r f; do
            echo "$(basename "$f") - $(du -sh "$f" | cut -f1)"
          done >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**How to Flash SD Card:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Download the artifact from this build" >> $GITHUB_STEP_SUMMARY
          echo "2. Extract the .img file" >> $GITHUB_STEP_SUMMARY
          echo "3. Flash to SD card using one of these methods:" >> $GITHUB_STEP_SUMMARY
          echo "   - Raspberry Pi Imager (easiest)" >> $GITHUB_STEP_SUMMARY
          echo "   - \`dd\` command: \`sudo dd bs=4M status=progress if=<downloaded-image.img> of=/dev/sdX\`" >> $GITHUB_STEP_SUMMARY
          echo "     (Replace \`<downloaded-image.img>\` with actual filename and \`/dev/sdX\` with your SD card device)" >> $GITHUB_STEP_SUMMARY
          echo "4. Insert SD card into LuckFox Pico device and boot!" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -n "$NAND_BUNDLES" ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 NAND Flash Bundles" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "$NAND_BUNDLES" | while IFS= read -r f; do
            echo "$(basename "$f") - $(du -sh "$f" | cut -f1)"
          done >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**How to Flash SPI-NAND:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Method 1: Windows - SocToolKit (Easiest)**" >> $GITHUB_STEP_SUMMARY
          echo "1. Download and install [SocToolKit](https://wiki.luckfox.com/Luckfox-Pico-Plus-Mini/Flash-image/#1-download-and-install-soctoolkit)" >> $GITHUB_STEP_SUMMARY
          echo "2. Extract the NAND bundle zip file" >> $GITHUB_STEP_SUMMARY
          echo "3. Connect device in MASKROM mode (hold BOOT button while connecting USB)" >> $GITHUB_STEP_SUMMARY
          echo "4. In SocToolKit: Select \`update.img\` and click Flash" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Method 2: Linux/Mac - rkdeveloptool**" >> $GITHUB_STEP_SUMMARY
          echo "1. Extract the NAND bundle zip file" >> $GITHUB_STEP_SUMMARY
          echo "2. Install rkdeveloptool: \`sudo apt install rkdeveloptool\` (Linux)" >> $GITHUB_STEP_SUMMARY
          echo "3. Connect device in MASKROM mode (hold BOOT button while connecting USB)" >> $GITHUB_STEP_SUMMARY
          echo "4. Flash individual partitions following \`sd_update.txt\` sequence" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Method 3: MicroSD Auto-Flash (No PC Connection)**" >> $GITHUB_STEP_SUMMARY
          echo "1. Extract NAND bundle and copy ALL files to MicroSD card root" >> $GITHUB_STEP_SUMMARY
          echo "2. Insert MicroSD into device and power on" >> $GITHUB_STEP_SUMMARY
          echo "3. U-Boot will automatically flash SPI-NAND from \`sd_update.txt\`" >> $GITHUB_STEP_SUMMARY
          echo "4. **Note:** This OVERWRITES existing SPI-NAND content" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Important:** NAND images are NOT the same as SD Card images!" >> $GITHUB_STEP_SUMMARY
          echo "- NAND bundles flash to onboard SPI-NAND storage" >> $GITHUB_STEP_SUMMARY
          echo "- SD Card images (\`.img\`) are for booting directly from SD" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📖 [Complete Flashing Guide](https://wiki.luckfox.com/Luckfox-Pico-Plus-Mini/Flash-image/)" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -n "$EMMC_BUNDLES" ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 eMMC Flash Bundles (Pico Pi)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**How to Flash eMMC:**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Method 1: Windows - SocToolKit (Easiest)**" >> $GITHUB_STEP_SUMMARY
          echo "1. Download and install [SocToolKit](https://wiki.luckfox.com/Luckfox-Pico-Plus-Mini/Flash-image/#1-download-and-install-soctoolkit)" >> $GITHUB_STEP_SUMMARY
          echo "2. Extract the eMMC bundle zip file" >> $GITHUB_STEP_SUMMARY
          echo "3. Connect device in MASKROM mode (hold BOOT button while connecting USB)" >> $GITHUB_STEP_SUMMARY
          echo "4. In SocToolKit: Select \`update.img\` and click Flash" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Method 2: Linux/Mac - rkdeveloptool**" >> $GITHUB_STEP_SUMMARY
          echo "1. Extract the eMMC bundle zip file" >> $GITHUB_STEP_SUMMARY
          echo "2. Install rkdeveloptool: \`sudo apt install rkdeveloptool\` (Linux)" >> $GITHUB_STEP_SUMMARY
          echo "3. Connect device in MASKROM mode (hold BOOT button while connecting USB)" >> $GITHUB_STEP_SUMMARY
          echo "4. Run: \`rkdeveloptool uf update.img\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📖 [Complete Flashing Guide](https://wiki.luckfox.com/Luckfox-Pico-Plus-Mini/Flash-image/)" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -z "$SD_IMAGES" ] && [ -z "$NAND_BUNDLES" ] && [ -z "$EMMC_BUNDLES" ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "❌ **No build artifacts found**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The build may have failed. Check the build logs above." >> $GITHUB_STEP_SUMMARY
        fi

  # Collect every combination's checksums into one manifest, mirroring the
  # sha256sum job in the seedsigner repo's build-buildroot.yml. Without a
  # published hash there is nothing for a third party to reproduce against, so
  # this is the deliverable that makes the reproducibility work checkable rather
  # than merely claimed.
  sha256sum:
    name: collect sha256 manifest
    runs-on: ubuntu-latest
    needs: build
    # `always()` so a partial matrix still publishes hashes for what did build;
    # skipped entirely if every combination failed.
    if: ${{ always() && needs.build.result != 'skipped' }}
    steps:
      - name: Download per-combination manifests
        uses: actions/download-artifact@v4
        with:
          pattern: sha256-*
          path: manifests

      - name: Merge into one manifest
        run: |
          set -euo pipefail
          shopt -s nullglob

          parts=(manifests/*/sha256sums.txt)
          if [ ${#parts[@]} -eq 0 ]; then
            echo "No checksum manifests were produced -- every build combination failed." >&2
            exit 1
          fi

          # Sorted so the merged manifest does not depend on artifact download
          # order, which is not stable.
          LC_ALL=C cat "${parts[@]}" | LC_ALL=C sort -k2 > seedsigner_luckfox.${GITHUB_SHA:0:7}.sha256

          echo "## Luckfox Pico image checksums" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          cat seedsigner_luckfox.${GITHUB_SHA:0:7}.sha256 >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          cat seedsigner_luckfox.${GITHUB_SHA:0:7}.sha256

      - name: Upload merged manifest
        uses: actions/upload-artifact@v4
        with:
          name: seedsigner_luckfox_images_sha256
          path: "*.sha256"
          if-no-files-found: error
          retention-days: 90
