# Force built-in for Amlogic GXL hardware required by the initramfs image
# (the arm64 arch default config compiles several of these as modules, which
# cannot be loaded from an initramfs that has no on-disk module tree)
CONFIG_SERIAL_MESON=y
CONFIG_SERIAL_MESON_CONSOLE=y
CONFIG_MMC_MESON_GX=y
CONFIG_SPI_MESON_SPICC=y
CONFIG_SPI_SPIDEV=y
CONFIG_HW_RANDOM_MESON=y

# USB host + UVC camera stack (La Frite uses a USB camera at /dev/video1).
# Forced built-in for the same reason as above: the arm64 arch default builds
# these as modules, and the initramfs has no on-disk module tree to load them
# from (matches the lafrite-smartcard-dev fragment).
CONFIG_USB_XHCI_HCD=y
CONFIG_USB_DWC3=y
CONFIG_USB_STORAGE=y
CONFIG_MEDIA_SUPPORT=y
CONFIG_MEDIA_CAMERA_SUPPORT=y
CONFIG_MEDIA_USB_SUPPORT=y
CONFIG_MEDIA_CONTROLLER=y
CONFIG_VIDEO_DEV=y
CONFIG_V4L2_MEM2MEM_DEV=y
CONFIG_USB_VIDEO_CLASS=y
CONFIG_UVC_COMMON=y
CONFIG_VIDEOBUF2_CORE=y
CONFIG_VIDEOBUF2_V4L2=y
CONFIG_VIDEOBUF2_VMALLOC=y

# Disable networking (non-dev production build - SeedSigner is air-gapped)
# CONFIG_INET is not set
# CONFIG_IPV6 is not set
# CONFIG_NETDEVICES is not set
# CONFIG_PACKET is not set
# CONFIG_STMMAC_ETH is not set
# CONFIG_DWMAC_MESON is not set
# CONFIG_MDIO_BUS_MUX_MESON_GXL is not set
# CONFIG_PHYLIB is not set
# CONFIG_MESON_GXL_PHY is not set

# Disable the wireless stack entirely (non-dev production build). La Frite has
# no on-board radio, but the arm64 arch default config builds these as
# loadable modules (CONFIG_CFG80211=m, CONFIG_MAC80211=m); left unset, the
# .ko files still ship on disk and the app's hardening self-check
# (check_wireless_stack/check_wifi_modules in helpers/hardening.py) correctly
# flags that as an open exposure — the driver stack could pair with any USB
# WiFi adapter on a compromised device even without a built-in radio.
# CONFIG_CFG80211 is not set
# CONFIG_MAC80211 is not set

# Disable display/HDMI output (SeedSigner uses external SPI LCD only)
# CONFIG_DRM is not set
# CONFIG_FRAMEBUFFER_CONSOLE is not set
# CONFIG_FB is not set

# Silence the kernel console: extlinux passes console=ttynull, and ttynull gives
# /dev/console a valid null backing so nothing leaks over the serial UART. The
# DTS still sets stdout-path=serial0, but a console= on the cmdline overrides it.
CONFIG_NULL_TTY=y
